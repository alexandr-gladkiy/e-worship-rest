const db = require("../../db");

class Sound {

    async CreateSound(sound_property) {
        //TODO: Create sound and return result
        //const newSound = await db.query('INSERT INTO')
    }

    async GetSounds() {
        //TODO: return all sound list
        //const newSound = await db.query('INSERT INTO')
    }

    async GetOneSound(id) {
        //TODO: return sound by id
        //const newSound = await db.query('INSERT INTO')
    }

    async UpdateSound(id, sound_property) {
        //TODO: Update sound property by id
        //const newSound = await db.query('INSERT INTO')
    }

    async DeleteSound(id) {
        //TODO: Delete sound by id
        //const newSound = await db.query('INSERT INTO')
    }
}